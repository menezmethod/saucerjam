services:
  game:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      NODE_ENV: production
    volumes:
      - rankings:/app/server/data
    restart: unless-stopped

volumes:
  rankings:
