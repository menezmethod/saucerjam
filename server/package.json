{
  "name": "saucerjam-server",
  "version": "1.2.0",
  "private": true,
  "description": "Use the root installation for the shared SaucerJam runtime",
  "scripts": {
    "start": "node server.js",
    "dev": "npm --prefix .. run dev",
    "test": "npm --prefix .. test"
  },
  "engines": {
    "node": ">=20"
  }
}
